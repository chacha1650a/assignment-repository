# 0812 온라인 포트폴리오 작업 내용 (김대훈)

## 개요
- **목적**: K-뉴딜 국비교육 과제 — 강사님께 보여드리기 위한 온라인 포트폴리오
- **형태**: 공개 호스팅 없이 로컬 HTML 파일로 진행 (본인 요청)
- **경로**: `C:\gov\K 뉴딜 과제(김대훈)\1번째 과제(온라인 포트폴리오)\`
  - `index.html`, `style.css`, `script.js`
- **여는 방법**: `index.html`을 더블클릭해서 브라우저로 열면 됨

---

## 카드 1 — 대상과 공개 범위
- 대상 문장: "이 페이지는 **강사님**에게 **K-뉴딜 국비교육 과제 결과물**을 보여드리기 위한 것이다"
- 이름: 김대훈 (실명 그대로 사용)
- **공개할 것**: GitHub 주소(github.com/chacha1650a), 기술 스택(React·Spring Boot·MySQL), 성격 강점(끝까지 파고드는 근성)
- **공개하지 않을 것**: 학교, 얼굴 사진, 거주지

## 카드 2 — 말이 아니라 근거
4인 팀 프로젝트 **Clov.**(친구와의 약속과 추억을 기록하는 웹 서비스, 2026.06.26~2026.08.14, GitHub: Pickeslog 조직)에서 상점(Shop) API를 담당한 경험을 바탕으로 강점 3개를 상황·행동·결과로 구체화:

1. **정보를 한곳에 모아 정리하는 힘** — Contract-First 방식으로 상점 API 명세·DB 설계를 문서화해 팀과 공유
2. **끝까지 포기하지 않는 근성** — 골드 잔액 동시성 문제를 원인부터 분석해 지갑 행 단위 잠금(row-level lock)으로 해결
3. **맡은 바를 성실히 수행하는 책임감** — 상점 도메인 전체(조회·구매·거래내역 API)를 기간 내 완성·배포

## 카드 3 — 첫 화면 재구성
- 1366×768: 스크롤 없이 소개(이름·태그라인)·활동(기술 뱃지)·근거(GitHub 버튼)가 모두 노출됨 확인
- 1920×1080: 본문 최대 폭(1100px) 고정 + 가운데 정렬로 과도한 여백/가로 스크롤 없음 확인
- GitHub 버튼에는 GitHub 로고(옥토캣) 아이콘을 넣고, 옆에 있던 "근거 보러 가기" 버튼은 제거해 CTA를 하나로 정리

## 카드 4 — 실제 결함 점검 및 수정 (4건)
브라우저 콘솔·명암 대비 계산·DOM 레이아웃을 직접 검사해 찾은 실제 결함:

| # | 결함 | 수정 전 | 수정 후 |
|---|------|---------|---------|
| 1 | 기본 버튼(흰 텍스트/인디고 배경) 명암 대비가 WCAG AA 기준(4.5:1) 미달 | 4.47:1 | 버튼 배경색 조정 → **6.29:1** |
| 2 | 다크 히어로 영역의 키보드 포커스 링이 배경과 잘 구분되지 않음(비텍스트 대비 3:1 기준 미달) | 2.38:1 | 히어로 전용 밝은 포커스 색상 적용 → **9.45:1** |
| 3 | 강점 카드 3개 중 1번 카드 요약문이 길어 "자세히 보기" 버튼 위치가 다른 카드보다 24px 아래로 밀림(정렬 불일치) | 버튼 Y좌표 불일치 | flex 레이아웃으로 버튼을 카드 하단에 통일 정렬 |
| 4 | 카드 1개만 펼쳐도 그리드가 같은 행 카드 높이를 서로 맞추는 바람에(`align-items` 기본값 stretch) 나머지 2개 카드까지 같이 늘어남 | 카드 1개 펼침 → 3개 전부 세로로 늘어남 | `.strength-list`에 `align-items: start` 지정 → 각 카드가 자기 콘텐츠 높이만큼만 차지하도록 수정 |

- 콘솔 오류 없음, 모든 리소스 200 OK 정상 로드 확인
- 제목 구조 h1 → h2 → h3 순서로 정상, 단계 건너뜀 없음
- 스킵 링크("본문 바로가기") 정상 동작 확인

## 카드 5 — 나와 연결된 재미
강점 카드 3개 전부에 "자세히 보기" 토글 버튼 추가 — 클릭하면 상황·행동·결과 상세 내용이 펼쳐짐(`aria-expanded`/`aria-controls`로 상태 관리, 네이티브 `<button>` 요소 사용).

- 펼침/접힘은 `hidden` 속성 대신 `max-height`+`opacity` 트랜지션으로 구현해 뚝 끊기지 않고 **부드럽게 슬라이드되며 나타남**
- 카드가 펼쳐진 동안에는 해당 카드 테두리에 **인디고 색 네온 글로우**가 입혀져 어떤 카드가 열려 있는지 한눈에 보임
- 접히면 자동으로 원래 상태(테두리 색·그림자)로 복귀

**⚠️ 참고 (테스트 환경 한계)**: 이번 세션의 브라우저 자동화 도구에서 마우스 클릭 좌표가 실제 페이지 좌표와 어긋나는 문제가 있어, 실제 클릭 시뮬레이션으로 100% 검증하지는 못했습니다. 대신 다음으로 정상 동작을 간접 확인했습니다.
- 버튼에 동일한 클릭 이벤트를 직접 발생시켜 토글 로직(펼침/접힘, 텍스트 변경, 높이·투명도 애니메이션, 네온 강조 클래스) 정상 동작 확인
- `<button type="button">`은 모든 표준 브라우저에서 Enter/Space로 기본 활성화되는 HTML 표준 동작이라 별도 구현 없이 보장됨
- Tab 키로 포커스가 버튼까지 정상 순서로 도달하는 것 확인
- **권장**: 제출 전 실제 브라우저에서 직접 한 번씩 클릭 및 Tab+Enter로 눌러 확인해보시는 것을 추천드립니다.

---

## 스타일 변경 이력
1. 1차: 미니멀 라이트 톤 + 인디고 포인트 컬러 1개 (과한 스타일 지양)
2. 사용자 피드백("밋밋하다, 현업 스타일 원함") 반영 → **다크 히어로 + 코드감** 스타일로 변경
   - 히어로 영역: 어두운 네이비 배경 + 실제 코드 에디터처럼 보이는 카드(구문 강조 포함)
   - 기술 스택 뱃지: 모노스페이스 폰트 + `</>`  아이콘
   - 본문(강점·근거) 섹션: 가독성을 위해 밝은 배경 유지
   - 카드에 그림자·호버 효과 추가
3. 추가 다듬기
   - 히어로 CTA를 "GitHub 바로가기"(옥토캣 아이콘 포함) 1개로 정리, "근거 보러 가기" 제거
   - 강점 카드 펼침/접힘을 즉시 전환 → `max-height`+`opacity` 트랜지션으로 부드럽게 애니메이션
   - 펼쳐진 카드는 인디고 네온 테두리로 강조
   - 그리드 정렬을 `stretch`(기본값)에서 `start`로 바꿔, 카드 1개를 펼쳐도 나머지 카드는 늘어나지 않도록 수정

## 남은 선택 사항
- 현재는 공개 호스팅 없이 로컬 파일로만 존재함 (지시사항의 "공개 주소" 통과 기준은 충족되지 않음)
- 추후 공개가 필요해지면 완성된 `index.html`/`style.css`/`script.js`를 그대로 무료 정적 호스팅(GitHub Pages 등)에 올리기만 하면 됨
