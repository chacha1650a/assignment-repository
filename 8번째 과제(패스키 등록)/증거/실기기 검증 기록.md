# 실기기 검증 기록 — 실제 브라우저·실제 기기

> ⚠️ **아직 채우지 않았습니다.** 배포를 마친 뒤 실제 기기에서 아래 순서대로 확인하고, `TODO` 를 채워 주세요.
> 자동으로 돌린 확인은 [`자동 검증 기록.md`](자동%20검증%20기록.md) 에 이미 전부 들어 있습니다.
> 이 파일이 따로 있는 이유는, 자동 기록만으로는 **"진짜 패스키 창이 뜨고 지문으로 통과했다"** 를 보일 수 없기 때문입니다.

| 항목 | 값 |
| --- | --- |
| 확인 날짜 | TODO |
| 결과물 주소 | https://chacha1650a.github.io/assignment-repository/8%EB%B2%88%EC%A7%B8%20%EA%B3%BC%EC%A0%9C(%ED%8C%A8%EC%8A%A4%ED%82%A4%20%EB%93%B1%EB%A1%9D)/index.html |
| 서버 | https://daehoon-passkey-backend.onrender.com |
| RP ID | `chacha1650a.github.io` |
| 기기 1 | TODO (예: 윈도우 11 노트북 · 크롬 · Windows Hello) |
| 기기 2 | TODO (예: 안드로이드 휴대폰 · 크롬 · 구글 비밀번호 관리자) |

---

## 확인 순서

### 0. 배포 확인

- [ ] Render 대시보드에서 `daehoon-passkey-backend` 서비스가 **Live** 인지 봅니다.
- [ ] `https://daehoon-passkey-backend.onrender.com/api/health` 를 열어 `rpId` 가 **`chacha1650a.github.io`** 인지 확인합니다.
      다르면 패스키 창이 아예 뜨지 않습니다.
- [ ] Render 가 실제로 준 주소가 위와 다르면, [`passkey.js`](../passkey.js) 21번째 줄의 주소 한 줄만 고치고 다시 올립니다.

> 남길 것: `/api/health` 응답 화면

### 1. 로그인 전 화면 (T08-C13 / C15 / C18)

- [ ] 새 시크릿 창으로 결과물 주소를 엽니다.
- [ ] 아래로 내려 **초록 "여기까지 공개" / 빨강 "여기부터 비공개"** 경계선과 짙은 남색 서랍이 갈라져 보이는지 봅니다.
- [ ] `Ctrl+U` 로 페이지 소스를 열고 `Ctrl+F` 로 `복구 코드`, `가상의 회사` 를 찾아 **한 건도 나오지 않는지** 확인합니다.
      (이 두 말은 서랍 안 항목에만 들어 있습니다. `회고` 같은 분류 이름은 잠긴 화면의 안내문에 있어서 검색됩니다)

> 남길 것: `화면-01-공개와 비공개 경계.png`, `화면-02-로그인 전 페이지 소스.png`
>
> 소스에서 찾은 결과: **TODO** (예: "복구 코드" 0건 / "가상의 회사" 0건)

### 2. 로그인 없이 서버에 직접 요청 (T08-C16 / C17)

- [ ] 새 탭에서 `https://daehoon-passkey-backend.onrender.com/api/private/items` 를 그대로 엽니다.

```
GET /api/private/items
(Authorization 헤더 없음)

응답: TODO   ← 401 이어야 합니다
본문: TODO   ← {"error":"unauthorized","message":"패스키로 들어와야 볼 수 있어요."}
```

> 남길 것: `화면-03-로그인 없이 401.png`

### 3. 첫 번째 패스키 등록 (T08-C19 ~ C26)

- [ ] 서랍의 **계정 이름**에 쓸 이름을 정합니다 → **TODO** (예: `daehoon`)
- [ ] **패스키 새로 만들기** 를 누릅니다. 패스키 창이 뜨고 지문·얼굴·PIN 을 확인합니다.
- [ ] 이름을 물어보면 알아볼 수 있게 적습니다 → **TODO** (예: `회사 노트북(Windows Hello)`)
- [ ] 브라우저 개발자 도구 **Network** 탭에서 `register/verify` 요청의 **Payload** 를 펼쳐,
      `privateKey` 같은 개인키가 **하나도 없는지** 눈으로 확인합니다. (T08-C23)
- [ ] **"지금 무슨 일이 일어나나요?"** 를 펼쳐 질문 값을 적고, 취소했다가 다시 눌러 **값이 바뀌는지** 봅니다. (T08-C20)

```
1회차 질문: TODO
2회차 질문: TODO   ← 서로 달라야 합니다
```

- [ ] 이 패스키가 **어디에 저장됐는지** 적습니다 (T08-C26) → **TODO**
      (구글 비밀번호 관리자 / 이 기기 자체(Windows Hello·Touch ID) / 보안 키 중 하나)

> 남길 것: `화면-04-패스키 등록 창.png`, `화면-05-등록 요청 본문(개인키 없음).png`

### 4. 등록 중간 취소 (T08-C25)

- [ ] **패스키 새로 만들기** 를 누르고, 패스키 창을 **그냥 닫습니다.**
- [ ] 화면에 **"취소했거나 시간이 지났습니다. 서버에는 아무것도 저장되지 않았습니다."** 가 뜨는지 봅니다.
- [ ] 패스키 목록의 개수가 **그대로인지** 확인합니다.

```
취소 전 패스키 수: TODO
취소 후 패스키 수: TODO   ← 같아야 합니다
```

> 남길 것: `화면-06-등록 취소 안내.png`

### 5. 로그인 (T08-C27 ~ C32)

- [ ] **패스키로 들어가기** 를 누르고 지문·얼굴·PIN 으로 통과합니다.
- [ ] 서랍이 열리고 비공개 항목 3건 이상이 보이는지 확인합니다. (T08-C14)
- [ ] Network 탭에서 `login/options` 를 두 번 눌러 `challenge` 값이 **매번 다른지** 봅니다. (T08-C28)

```
1회차 로그인 질문: TODO
2회차 로그인 질문: TODO   ← 서로 달라야 합니다
```

> 남길 것: `화면-07-로그인 뒤 열린 서랍.png`

### 6. 두 번째 패스키 등록 (T08-C42 / C43)

- [ ] **다른 기기(또는 다른 브라우저)** 에서 같은 계정 이름으로 먼저 로그인합니다.
      (같은 기기에서 또 등록하려 하면 "이 기기에는 이 계정의 패스키가 이미 있습니다"가 뜹니다 — 이것도 정상 동작입니다)
- [ ] **이 기기의 패스키 추가 등록** 을 누르고 이름을 붙입니다 → **TODO** (예: `개인 휴대폰(구글 비밀번호 관리자)`)
- [ ] 목록에 **두 개**가 각각 이름과 등록 날짜와 함께 보이는지 확인합니다.

> 남길 것: `화면-08-패스키 두 개 목록.png`

### 7. 하나 지운 뒤 로그인 (T08-C44 / C45 / C46)

- [ ] 두 번째 패스키의 **지우기** 를 누릅니다.
- [ ] **나가기** 를 누른 뒤, **남은 첫 번째 패스키로** 다시 들어가 봅니다. → 들어가지면 통과.
- [ ] **지운 쪽 기기**에서 로그인해 봅니다. (기기에는 개인키가 그대로 남아 있습니다)

```
남은 패스키로 로그인:   TODO   ← 200
지운 패스키로 로그인:   TODO   ← 401 unknown_credential
```

- [ ] 하나 남은 패스키의 **지우기** 를 눌러 봅니다. → **"마지막 패스키는 지울 수 없어요."** (409) 가 뜨는지 확인합니다. (T08-C46)

> 남길 것: `화면-09-하나 지운 뒤 목록.png`, `화면-10-마지막 패스키 삭제 거절.png`

### 8. 두 번째 계정과 서로의 자료 (T08-C36 ~ C40)

- [ ] 다른 브라우저(또는 시크릿 창)에서 **두 번째 계정 이름**으로 패스키를 새로 등록합니다 → **TODO** (예: `tester-b`)
- [ ] 그 계정의 서랍에 **다른 내용**을 하나 넣습니다.
- [ ] 개발자 도구 **Console** 에서, **A 계정의 토큰으로** B 계정을 적어 요청해 봅니다.

```js
// A 계정으로 로그인한 탭의 콘솔에서
await fetch('https://daehoon-passkey-backend.onrender.com/api/private/items?handle=tester-b', {
  headers: {
    Authorization: 'Bearer ' + sessionStorage.getItem('passkey-vault-token'),
    'X-User-Handle': 'tester-b'
  }
}).then(r => r.json());
```

```
돌아온 owner: TODO   ← A 계정 이름이어야 합니다 (B의 자료는 한 건도 안 옵니다)
```

- [ ] 반대 방향(B 탭에서 A를 적어 요청)도 똑같이 해 봅니다. (T08-C38)

```
돌아온 owner: TODO   ← B 계정 이름이어야 합니다
```

- [ ] 거절 앞뒤로 상대편 자료 건수가 그대로인지 확인합니다. (T08-C39)

```
| 계정 | 거절 전 | 거절 후 |
| A    | TODO건  | TODO건  |
| B    | TODO건  | TODO건  |
```

> 남길 것: `화면-11-남의 계정 적어 보낸 요청과 응답.png`

### 9. 로그아웃 (T08-C33)

- [ ] **나가기** 를 누르고, 곧바로 같은 토큰으로 다시 요청해 봅니다.

```js
// 나가기 전에 토큰을 따로 담아 뒀다가
await fetch('https://daehoon-passkey-backend.onrender.com/api/private/items', {
  headers: { Authorization: 'Bearer ' + 미리담아둔토큰 }
}).then(r => r.status);
```

```
로그아웃 전: TODO   ← 200
로그아웃 후: TODO   ← 401
```

> 남길 것: `화면-12-로그아웃 뒤 같은 토큰 401.png`

---

## 배포본에 대고 자동 확인 다시 돌리기

배포가 끝났으면, 자동 확인도 **배포한 서버에 대고** 한 번 더 돌려 두면 좋습니다.

```bash
cd passkey-backend
node test-virtual-authenticator.js \
  --base https://daehoon-passkey-backend.onrender.com \
  --rp-id chacha1650a.github.io \
  --origin https://chacha1650a.github.io \
  --out "../8번째 과제(패스키 등록)/증거/배포 서버 검증 기록.md"
```

> ⚠️ 이 스크립트는 서버에 **테스트 계정과 패스키를 실제로 만듭니다.** 배포본에 남는 것이 싫으면 로컬에서만 돌리세요.

결과: **TODO** (통과 개수 / 전체 개수)

---

## 확인하면서 찾아낸 것

> 실제로 눌러 보다 발견한 문제와 고친 내용을 여기 적습니다. 없으면 "없었습니다"라고 적습니다.

- TODO
